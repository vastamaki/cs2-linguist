{
  "name": "cs2-linguist",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "test": "bun test",
    "tauri": "tauri",
    "desktop": "bun run scripts/build-workers.ts cpu && tauri dev",
    "package:windows": "bun run scripts/build-workers.ts all && tauri build --target x86_64-pc-windows-msvc --config src-tauri/tauri.bundle.conf.json"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0",
    "@types/bun": "^1.3.0",
    "typescript": "~5.9.3",
    "vite": "^7.3.0"
  }
}
